{
  "name": "wasm-spec-gen",
  "private": true,
  "type": "module",
  "description": "Generator for the pinned WebAssembly wg-1.0 spec-test artifacts (issue #141). Acquire inputs through Home's fetch.ts, then run gen.mjs.",
  "dependencies": {
    "wabt": "^1.0.39"
  }
}
